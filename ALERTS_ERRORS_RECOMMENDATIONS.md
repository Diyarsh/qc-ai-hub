# Рекомендации по алертам и ошибкам для AI Hub UI

## 🎯 Общая стратегия

### Принципы обработки ошибок

1. **Пользователь должен понимать, что произошло** - ясные сообщения
2. **Предлагать решение проблемы** - не просто ошибка, а что делать
3. **Разные уровни критичности** - разные типы уведомлений
4. **Не перегружать пользователя** - не показывать технические детали
5. **Контекстная информация** - показывать ошибки там, где они произошли

---

## 📊 Типы уведомлений и их использование

### 1. **Toast Notifications** (Всплывающие уведомления)

**Использование:** Краткие уведомления о действиях пользователя или автоматических событиях

#### ✅ **Success (Зеленый)**
- **Когда использовать:**
  - Успешное сохранение данных
  - Успешная отправка сообщения
  - Успешное выполнение workflow
  - Успешная загрузка файла
  - Успешная операция экспорта/импорта

- **Примеры:**
  ```typescript
  showToast('Workflow сохранен успешно', 'success');
  showToast('Сообщение отправлено', 'success');
  showToast('Файл загружен', 'success');
  ```

#### ❌ **Error (Красный)**
- **Когда использовать:**
  - Ошибка при сохранении
  - Ошибка отправки сообщения
  - Ошибка выполнения workflow
  - Ошибка загрузки данных
  - Ошибка валидации формы

- **Примеры:**
  ```typescript
  showToast('Ошибка при сохранении workflow', 'error');
  showToast('Не удалось отправить сообщение. Проверьте подключение', 'error');
  showToast('Ошибка выполнения: узел не настроен', 'error');
  ```

#### ⚠️ **Warning (Оранжевый)**
- **Когда использовать:**
  - Предупреждения о потенциальных проблемах
  - Потери данных (например, при удалении)
  - Превышение лимитов
  - Устаревшие данные
  - Рекомендации по улучшению

- **Примеры:**
  ```typescript
  showToast('Вы достигли лимита запросов на сегодня', 'warning');
  showToast('Workflow не сохранен. Сохранить перед выходом?', 'warning');
  showToast('Используется устаревшая версия модели', 'warning');
  ```

#### ℹ️ **Info (Синий)**
- **Когда использовать:**
  - Информационные сообщения
  - Подсказки
  - Обновления статуса
  - Прогресс операций

- **Примеры:**
  ```typescript
  showToast('Workflow выполняется...', 'info');
  showToast('Данные обновлены', 'info');
  showToast('Новая версия доступна', 'info');
  ```

---

### 2. **Inline Errors** (Встроенные ошибки в формах)

**Использование:** Ошибки валидации полей ввода

- **Расположение:** Под полем ввода или рядом с ним
- **Стиль:** Красный текст, иконка AlertCircle
- **Пример:**
  ```tsx
  <div className="text-sm text-destructive mt-1 flex items-center gap-1">
    <AlertCircle className="h-4 w-4" />
    Это поле обязательно для заполнения
  </div>
  ```

**Типы ошибок:**
- Обязательное поле не заполнено
- Неверный формат (email, URL, телефон)
- Превышена максимальная длина
- Неверное значение (например, дата в прошлом)
- Уникальность (например, имя уже используется)

---

### 3. **Error Panels** (Панели ошибок)

**Использование:** Детальные ошибки с контекстом и решениями

**Когда использовать:**
- Ошибки выполнения workflow (Laboratory)
- Критические ошибки, требующие внимания
- Ошибки с возможностью исправления

**Структура ErrorPanel:**
1. **Заголовок** с иконкой AlertCircle
2. **Детали ошибки** - понятное описание
3. **Предложенные решения** - карточки с действиями
4. **Кнопка Retry** - повторная попытка
5. **Кнопка закрытия**

**Пример использования:**
```typescript
// В Laboratory3.0 при ошибке workflow
<ErrorPanel
  error="Узел 'LLM Node' не настроен: отсутствует API ключ"
  onClose={() => setError(null)}
  onAddNode={(node) => {
    // Добавить узел обработки ошибок
  }}
/>
```

---

### 4. **Error Boundary** (Граница ошибок)

**Использование:** Критические ошибки React, которые ломают интерфейс

**Когда использовать:**
- Необработанные ошибки рендеринга
- Ошибки компонентов верхнего уровня
- Сетевые ошибки, которые не могут быть обработаны

**Структура:**
- Краткое сообщение об ошибке
- Кнопка "Перезагрузить страницу"
- Опционально: кнопка "Сообщить об ошибке"

---

### 5. **Banner Alerts** (Баннеры-уведомления)

**Использование:** Важные сообщения, которые должны быть видны постоянно

**Типы:**

#### 🔴 **Критический баннер**
- Проблемы с подключением к API
- Проблемы с аутентификацией
- Критические системные ошибки

#### 🟡 **Предупреждающий баннер**
- Плановое обслуживание
- Превышение лимитов
- Устаревшие данные

#### 🔵 **Информационный баннер**
- Новые функции
- Обновления системы
- Рекомендации

**Пример:**
```tsx
<Alert variant="destructive" className="mb-4">
  <AlertCircle className="h-4 w-4" />
  <AlertTitle>Не удается подключиться к серверу</AlertTitle>
  <AlertDescription>
    Проверьте подключение к интернету и попробуйте снова.
  </AlertDescription>
</Alert>
```

---

### 6. **Loading States с ошибками**

**Использование:** Ошибки при загрузке данных

**Состояния:**
- Loading → Skeleton или Spinner
- Success → Данные
- Error → Error state с кнопкой "Повторить"

**Пример:**
```tsx
{isLoading && <Skeleton />}
{error && (
  <div className="text-center py-8">
    <AlertCircle className="h-12 w-12 text-destructive mx-auto mb-4" />
    <p className="text-destructive mb-4">Не удалось загрузить данные</p>
    <Button onClick={refetch}>Повторить</Button>
  </div>
)}
{data && <DataDisplay data={data} />}
```

---

## 🎨 Стилизация и компоненты

### Используемые компоненты из shadcn/ui:

1. **Toast** - для всплывающих уведомлений
2. **Alert** - для баннеров и важных сообщений
3. **AlertDialog** - для критических диалогов подтверждения

### Цветовая схема:

- **Success (Зеленый):** `bg-green-600`, `text-green-50`
- **Error (Красный):** `bg-red-600`, `text-red-50`, `text-destructive`
- **Warning (Оранжевый):** `bg-orange-500`, `text-orange-50`
- **Info (Синий):** `bg-blue-600`, `text-blue-50`

---

## 📝 Рекомендации по текстам сообщений

### ✅ Правильно:
- "Workflow сохранен успешно"
- "Не удалось отправить сообщение. Проверьте подключение к интернету"
- "Поле 'Email' обязательно для заполнения"
- "Узел 'LLM Node' не настроен: отсутствует API ключ"

### ❌ Неправильно:
- "Error 500"
- "Что-то пошло не так"
- "Ошибка"
- "Failed to save"

### Принципы написания сообщений:

1. **Понятно** - простой язык, без технических терминов
2. **Конкретно** - что именно произошло
3. **Полезно** - что делать дальше
4. **Кратко** - максимум 1-2 предложения

---

## 🔄 Сценарии использования по модулям

### Dashboard (Чат)
- **Toast Error:** Ошибка отправки сообщения
- **Toast Info:** Сообщение отправляется...
- **Toast Success:** Сообщение отправлено
- **Banner:** Проблемы с подключением к API

### AI Studio
- **Toast Warning:** Достигнут лимит запросов
- **Toast Error:** Ошибка выбора модели
- **Inline Error:** Неверный формат API ключа

### Laboratory2.0 / Laboratory3.0
- **Toast Success:** Workflow сохранен
- **Toast Error:** Ошибка выполнения workflow
- **ErrorPanel:** Детальные ошибки узлов с решениями
- **Toast Warning:** Workflow не сохранен

### Projects
- **Toast Success:** Проект создан
- **Toast Error:** Ошибка загрузки проекта
- **AlertDialog:** Подтверждение удаления проекта

### History
- **Toast Info:** История обновлена
- **Error State:** Ошибка загрузки истории

---

## 🛠️ Технические рекомендации

### 1. Централизованная обработка ошибок

```typescript
// services/errorHandler.ts
export class ErrorHandler {
  static handle(error: Error, context: string) {
    // Логирование
    console.error(`[${context}]`, error);
    
    // Отправка в систему мониторинга (Sentry, etc.)
    
    // Показ пользователю
    const userMessage = this.getUserFriendlyMessage(error);
    showToast(userMessage, 'error');
  }
  
  static getUserFriendlyMessage(error: Error): string {
    // Маппинг технических ошибок на понятные сообщения
    if (error.message.includes('network')) {
      return 'Ошибка подключения. Проверьте интернет';
    }
    // ...
  }
}
```

### 2. Типизация ошибок

```typescript
enum ErrorType {
  NETWORK = 'network',
  VALIDATION = 'validation',
  AUTH = 'auth',
  SERVER = 'server',
  UNKNOWN = 'unknown'
}

interface AppError {
  type: ErrorType;
  message: string;
  code?: string;
  details?: any;
}
```

### 3. React Query Error Handling

```typescript
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      onError: (error) => {
        ErrorHandler.handle(error, 'Query');
      },
    },
    mutations: {
      onError: (error) => {
        ErrorHandler.handle(error, 'Mutation');
      },
    },
  },
});
```

### 4. Валидация форм

```typescript
// Использовать react-hook-form с кастомными сообщениями
const schema = z.object({
  email: z.string().email('Неверный формат email'),
  name: z.string().min(3, 'Минимум 3 символа'),
});

// Показывать ошибки inline
{errors.email && (
  <p className="text-sm text-destructive mt-1">
    {errors.email.message}
  </p>
)}
```

---

## 📋 Чеклист для разработчиков

При добавлении новой функции, убедитесь:

- [ ] Все ошибки показываются пользователю понятным языком
- [ ] Успешные действия показывают Toast Success
- [ ] Ошибки валидации показываются inline в формах
- [ ] Критические ошибки используют ErrorPanel или Banner
- [ ] Ошибки сети обрабатываются отдельно
- [ ] Loading states показываются для долгих операций
- [ ] Есть возможность повторить действие при ошибке
- [ ] Технические детали не показываются пользователю
- [ ] Сообщения переведены на все поддерживаемые языки

---

## 🚀 Примеры реализации

### Пример 1: Обработка ошибки API

```typescript
try {
  const response = await api.saveWorkflow(data);
  showToast('Workflow сохранен успешно', 'success');
} catch (error) {
  if (error.response?.status === 401) {
    showToast('Сессия истекла. Войдите снова', 'error');
  } else if (error.response?.status === 500) {
    showToast('Ошибка сервера. Попробуйте позже', 'error');
  } else {
    showToast('Не удалось сохранить workflow', 'error');
  }
}
```

### Пример 2: Валидация формы

```tsx
<Input
  {...register('email', {
    required: 'Email обязателен',
    pattern: {
      value: /^[A-Z0-9._%+-]+@[A-Z0-9.-]+\.[A-Z]{2,}$/i,
      message: 'Неверный формат email'
    }
  })}
/>
{errors.email && (
  <div className="text-sm text-destructive mt-1 flex items-center gap-1">
    <AlertCircle className="h-4 w-4" />
    {errors.email.message}
  </div>
)}
```

### Пример 3: Error Boundary с деталями

```tsx
<ErrorBoundary
  fallback={
    <ErrorFallback
      error={error}
      onRetry={() => window.location.reload()}
      onReport={() => {
        // Отправить отчет об ошибке
      }}
    />
  }
>
  {children}
</ErrorBoundary>
```

---

## 📚 Дополнительные ресурсы

- [shadcn/ui Alert](https://ui.shadcn.com/docs/components/alert)
- [shadcn/ui Toast](https://ui.shadcn.com/docs/components/toast)
- [React Error Boundaries](https://react.dev/reference/react/Component#catching-rendering-errors-with-an-error-boundary)


